Implement struct A with two overloads of the prefix operator:

* for lvalues that results in an lvalue,

* for rvalues that results in an rvalue.

First, implement the operatos as member, then as non-member.
