template <typename T>
void foo(const T &t);

// Specialization #1: doesn't specialize the primary template.

// template <>
// void foo(bool);

// Specialization #2: doesn't specialize the primary template.

// template <>
// void foo(int &);
